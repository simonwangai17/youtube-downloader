function fetchVideoInfo() {
    var videoUrl = document.getElementById('videoUrl').value;
    // Here, you need to implement logic to fetch video information from YouTube using APIs (e.g., YouTube Data API).
    // Display fetched video information in #videoInfo div.
}

function startDownload() {
    // Here, you need to implement logic to start the download using IDM.
    // You can use a backend server (PHP) to handle the download process and initiate the download with IDM.
}
